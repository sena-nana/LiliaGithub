# LiliaGithub 1.0 稳定性验收记录

- 日期：2026-07-11
- Issue：[完成 1.0 发布稳定性验收 #10](https://github.com/sena-nana/LiliaGithub/issues/10)
- 应用源码基线：`a9cee5d`
- 安装包：`LiliaGithub_1.0.0_x64-setup.exe`
- SHA256：`4BE142CF526D995CAD239E54DAD981280788A0BEC2F983497A3733F594964874`
- 环境：Windows 11 专业版 Insider Preview `10.0.26300`、WebView2 `150.0.4078.48`
- 工具链：Node.js `24.13.0`、Yarn `4.14.1`、Rust/Cargo `1.96.0`、Git `2.53.0.windows.1`

## 结论

Issue #10 列出的安装、启动、工作区扫描、仓库详情、提交/pull/push、GitHub 授权、Issue/PR/Actions/Release 和卸载流程均已完成回归，结果全部为 PASS。验收后重新安装同一候选包并启动应用，恢复为验收前的已安装状态；Roaming 和 Local 用户数据在卸载期间均保留。

## 验收矩阵

| 验收项 | 结果 | 操作与证据 |
| --- | --- | --- |
| 安装 | PASS | `yarn tauri:build` 生成当前 NSIS 安装包；静默安装返回 `0`，安装目录、主程序、卸载程序和 HKCU 卸载注册表项均存在。 |
| 启动 | PASS | 从 `%LOCALAPPDATA%\LiliaGithub\lilia_github.exe` 启动；进程持续运行且 `Responding=True`，主窗口标题为 `LiliaGithub`，窗口句柄非零。 |
| 工作区扫描 | PASS | 真实安装版恢复现有工作区并显示 28 个仓库；仓库列表包含分支、改动计数和刷新状态。Rust workspace 测试同时覆盖工作区发现、managed repo、watcher 和 linked worktree。 |
| 仓库详情 | PASS | 真实安装版打开仓库详情并显示项目、文件树、变更、历史、Stash、分支、拉取和推送入口；完整桌面 replay 逐页验证对应路由和稳定目标。 |
| 提交 / pull / push | PASS | `cargo test --workspace --manifest-path src-tauri/Cargo.toml` 在隔离的真实 Git 临时仓库和 bare remote 上通过 stage/commit、pull、push、首次发布分支、upstream、非快进保护、dirty worktree、worktree 与冲突流程。 |
| GitHub 授权 | PASS | 真实安装版显示 `GitHub 已授权` 和账号 `sena-nana`；系统 keyring 凭证可复用，`gh auth status` 及在线 `user` API 调用成功。完整桌面 replay 验证设置页授权入口。 |
| Issue | PASS | 在线 API 成功读取 Issue；桌面 replay 打开 Issue 列表与创建表单并取消，稳定目标无缺失。 |
| PR | PASS | 在线 API 成功读取 Pull Request；桌面 replay 打开 PR 列表与创建表单并取消，稳定目标无缺失。 |
| Actions | PASS | 在线 API 成功读取最近 workflow run；桌面 replay 打开 Actions 页面；后端和前端测试覆盖 run/job 详情、日志、artifact、失败诊断与重跑。 |
| Release | PASS | 在线 API 成功读取 Release；桌面 replay 打开 Release 列表、筛选和创建表单并取消；生产 NSIS bundle 构建成功。 |
| 卸载 | PASS | 关闭应用后运行 `%LOCALAPPDATA%\LiliaGithub\uninstall.exe /S`，返回 `0`；进程归零、安装目录和 HKCU 卸载项均清除，Roaming/Local 用户数据保留。随后重新安装同一 SHA256 安装包并成功启动。 |

## 自动化与构建证据

- `yarn install --immutable`：通过。
- `node --check agent-debug/verify-agent-debug.mjs`：通过。
- `yarn test tests/tooling.test.ts`：4/4 通过。
- `yarn test tests/titleBar.test.ts`：5/5 通过。
- `yarn test tests/repoProjectPanel.test.ts`：63/63 通过。
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`：168/168 通过。
- `yarn agent-debug:verify`：通过；run id `2026-07-11T05-28-43-880Z`，24 个页面/状态快照，`missingAgentIdCount=0`，桌面 replay 未跳过。
- `yarn tauri:build`：通过；生成上述 NSIS 安装包。
- GitHub 在线读取：账号、Issue、PR、Actions run 和 Release 均成功。

## 回归期间修复

- `c3ec922`：修复仓库刷新自激循环。
- `c747c77`：补全失败 workflow 诊断与重跑。
- `a9cee5d`：清理已移除的命令面板与更新检查残留。
- Agent debug 验证器区分基础 readiness 与桌面 replay，并支持显式指定本机 WebDriver；本次验收使用匹配 WebView2 的 EdgeDriver 完整执行 replay。

## 已知测试基础设施说明

`tests/router.test.ts` 的整文件批量运行仍存在同进程跨用例异步状态污染：首个用例后会出现级联超时，而相同失败项独立运行通过。该现象没有出现在真实安装版、完整桌面 replay、仓库详情组件测试或 Rust/Git 流程测试中，因此不改变上述产品流程的 PASS 结论；本次没有用放宽断言或跳过测试来隐藏该现象。
